

type Props={
    activeCourses:any; //will be replace by the DB types
    hearts:number;
    points:number;
    hasActiveSubscription:boolean;
}


const UserProgress=({activeCourses,hearts,points, hasActiveSubscription}:Props)=>{
    
    return(
        <div>

        </div>
    )
}

export default UserProgress